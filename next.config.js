/** @type {import('next').NextConfig} */
const nextConfig = {};
module.exports = {
  images: {
    domains: ["localhost:3000"], // Add the hostname(s) where your images are hosted
  },
};
module.exports = nextConfig;
